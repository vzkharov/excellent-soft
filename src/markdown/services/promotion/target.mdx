import { Card } from '~/components/ui/card'
import { Spacer } from '~/components/ui/spacer'

Контекстная реклама нужна для привлечения целевой аудитории к вашему продукту или услуге. Работает через платные объявления,
которые отображаются в поисковых системах и на других платформах на основе релевантности ключевых слов и интересов пользователей.

<Spacer y="2xl" />

##### Процесс настройки рекламы

<Spacer y="md" />

<div className="grid grid-cols-1 gap-1.5 md:grid-cols-2">
	{[
		'Сбор семантического ядра',
		'Анализ объявлений конкурентов',
		'УТП',
		'Создание и заполнение рекламных кабинетов',
		'Запуск и тестирование кампаний',
		'Анализ результата',
	].map((item, idx) => (
		<Card
			key={idx}
			idx={idx + 1}
			title={item}
			size="md"
			variant="default"
		/>
	))}
</div>
